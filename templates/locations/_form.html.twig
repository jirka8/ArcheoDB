{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

<div>{{ form_errors(form) }}</div>

<div>
    {{ form_label(
        form.title,
        'Název lokality',
        {'label_attr': {
            'class': 'block text-md'
        }}    
    ) }}
    {{ form_widget(
        form.title,
        {'attr': {
            'class': 'block w-full shadow-sm border-gray-300  rounded-md border p-2 mt-1 mb-2'
        }}
    ) }}
    {{ form_errors(form.title) }}
</div>

<div>
    {{ form_label(
        form.description,
        'Popis lokality',
        {'label_attr': {
            'class': 'block text-md'
        }}
    ) }}
    {{ form_widget(
        form.description,
        {'attr': {
            'class': 'block w-full shadow-sm border-gray-300  rounded-md border p-2 mt-1 mb-2'
        }}
    ) }}
    {{ form_errors(form.description) }}
</div>

<div>
    <button type="submit" 
                class="block w-full shadow-sm border-transparent bg-green-600 hover:bg-green-700 text-white rounded-md border p-2 mt-4 mb-2">
                Uložit lokalitu</button>
</div>


{{ form_end(form) }}